package de.dhbw.exceptions;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

public class ExceptionsExerciseTests {

    @Test
    @DisplayName("""
        Übung 1: Exception werfen und fangen
        - Implementiere das Interface Executable mit einer Methode void execute(), dazu eine Klasse Task, die das
        Interface implementiert
        - Implementiere dazu auch eine neue konkrete, eigene Exception, die von CheckedException abgeleitet werden
        soll und von execute() geworfen werden kann.
        - Wirf diese neue Exception einfach mittels throws in der konkreten Methode execute() deiner konkreten Klasse.
        - Schreibe nun einen Unit-Test, der die Methode execute() aufruft und die geworfene Exception fängt und behandelt.
    """)
    public void exercise1() {
        // given

        // when

        // then
    }

}